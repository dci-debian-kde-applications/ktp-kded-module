#!/usr/bin/make -f

%:
	dh $@ --with kf5

override_dh_auto_configure:
	$(overridden_command) -- -DDBUS_SEND_PATH=/usr/bin/dbus-send

override_dh_strip:
	$(overridden_command) --dbg-package=kde-telepathy-integration-module-dbg
